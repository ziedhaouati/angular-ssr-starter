pipeline {
    agent any
    stages {
        stage('Install') {
            steps {
                sh "npm install"
            }
        }
        stage('Build') {
            steps {
                sh "npm run build:ssr"
            }
        }
         stage('Deploy') {
            steps {
                 sh "pm2 restart all"
            }
        }
    }
}
